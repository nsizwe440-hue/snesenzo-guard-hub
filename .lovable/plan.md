## Goal\nAdd visual weight to the middle of the homepage without changing structure, copy, or adding new sections. Three targeted enhancements to existing sections.\n\n## Changes\n\n### 1. Who We Are — 2-column with side image\nFile: `src/routes/index.tsx`, the WHO WE ARE section.\n\n- Convert from single-column text to `grid lg:grid-cols-2 gap-12 items-center`\n- Left column: existing eyebrow + heading + paragraph (unchanged copy)\n- Right column: an officer / on-duty photo, `aspect-[4/5] object-cover rounded-2xl shadow-lg`\n- Reuse an existing asset (e.g. `peopleDeployed` or `actionRetail`) so no new image is needed\n- On mobile, image stacks below text (default grid behavior)\n\n### 2. Four Pillars — photo background band\nFile: `src/routes/index.tsx`, the PILLARS section (currently flat `bg-brand-navy`).\n\n- Replace flat navy with a layered background: a dark officer/team photo + navy gradient overlay so text stays readable\n  - `background-image: linear-gradient(135deg, rgba(6,16,22,0.92), rgba(6,16,22,0.78)), url(<photo>)`\n  - `background-size: cover; background-position: center;`\n- Reuse an existing image (likely `peopleDisciplined` or `heroImg`)\n- Cards stay the same — bump card backgrounds slightly (`bg-white/[0.04]`) so they read clearly over the photo\n- Keep all spacing, grid, and content identical\n\n### 3. Services — top 3 cards become image cards\nFile: `src/routes/index.tsx`, the SERVICES grid.\n\n- First 3 services (VIP Protection, Events Security, CCTV & Armed Response) render as **image cards** instead of icon cards\n  - Same `aspect-[4/5]` style as the \